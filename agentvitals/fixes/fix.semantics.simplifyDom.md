# AgentVitals fix: Simplify DOM structure

**Failure class:** `fix.semantics.simplifyDom`  ·  **Severity:** high  ·  **Occurrences:** 1

## Why agents get stuck
Excessive DOM depth consumes LLM context and makes element selection unreliable.

## Where (sampled)
- `page`

## Suggested change
```
Reduce nesting depth; use CSS grid/flex instead of wrapper divs; prune unnecessary nodes.
```

_Generated by AgentVitals. Review, apply in your codebase, and the CI gate will confirm the regression is fixed._